\documentclass[12pt,a4paper]{article}

% --- Page geometry (guidelines: top 20 mm, bottom 20 mm, left 20 mm, right 10 mm) ---
\usepackage[top=20mm, bottom=20mm, left=20mm, right=10mm]{geometry}

% --- Typography: Times New Roman 12 pt, 1.5 line spacing ---
\usepackage[T1]{fontenc}
\usepackage{mathptmx}
\usepackage{setspace}
\usepackage{indentfirst}
\onehalfspacing

% --- Paragraphs: justified, 1.25 cm first-line indent, no extra space ---
\setlength{\parindent}{1.25cm}
\setlength{\parskip}{0pt}

% --- Packages ---
\usepackage{xcolor}
\usepackage{microtype}
\usepackage{titlesec}
\usepackage{caption}
\usepackage{graphicx}
\usepackage{courier}
\usepackage{listings}
\usepackage{amsmath,amssymb}
\usepackage{fancyhdr}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{float}
\usepackage{enumitem}
\usepackage{etoolbox}
\usepackage{adjustbox}
\usepackage{tikz}
\input{diagrams/styles.tex}
\usepackage{xurl}
\usepackage[hidelinks]{hyperref}

% --- Page numbers: bottom centre ---
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\fontsize{12pt}{14pt}\selectfont\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% --- Figures, tables and formulas are numbered within the chapter (1.1, 1.2, ...) ---
\numberwithin{equation}{section}
\numberwithin{figure}{section}
\numberwithin{table}{section}

% --- Chapter titles: new page, uppercase, bold, 13 pt, centred ---
% \MakeUppercase sits in titlesec's before-code slot (4th argument): only there
% does it receive the title text as its argument. The page break is issued as a
% plain \clearpage before each \section in the body, because a \clearpage fired
% from inside the title format is not yet shipped out when \addcontentsline reads
% \thepage, which would put a page number one too low into the table of contents.
\titleformat{\section}[block]
  {\centering\fontsize{13pt}{16pt}\bfseries}
  {\thesection}{1em}{\MakeUppercase}
\titlespacing*{\section}{0pt}{0pt}{14pt}

% --- Subchapter titles: bold, 12 pt, left-aligned, indented 1.25 cm ---
\titleformat{\subsection}[block]
  {\fontsize{12pt}{15pt}\bfseries}
  {\hspace*{1.25cm}\thesubsection}{1em}{}
\titlespacing*{\subsection}{0pt}{14pt}{8pt}

% \subsec{...} replaces \subsection{...}: every subchapter except the first one of a
% chapter starts on a new page, so two subchapters never share a page.
\newcommand{\subsec}[1]{%
  \ifnum\value{subsection}>0\clearpage\fi
  \subsection{#1}%
}

% --- Unnumbered chapters (Introduction, Conclusions, References, ...) ---
\newcommand{\unnumberedsection}[1]{%
  \clearpage
  \phantomsection
  \addcontentsline{toc}{section}{#1}
  \begin{center}
    {\fontsize{13pt}{16pt}\bfseries\MakeUppercase{#1}\par}
  \end{center}
  \vspace{14pt}
}
% Heading used for preliminary pages that are not listed in the table of contents.
\newcommand{\plainheading}[1]{%
  \clearpage
  \begin{center}
    {\fontsize{13pt}{16pt}\bfseries\MakeUppercase{#1}\par}
  \end{center}
  \vspace{14pt}
}

% --- Table of contents: our own heading, no built-in "Contents" heading ---
\makeatletter
\renewcommand{\tableofcontents}{{\setstretch{1.15}\@starttoc{toc}}}
\makeatother

% --- Captions ---
% Figures: "Figure 1.1 – Title", bold, centred, below the figure, no final period.
\captionsetup[figure]{labelfont=bf, textfont=bf, labelsep=endash,
  justification=centering, position=bottom, skip=8pt}
% Tables: "Table 1.1 – Title", above the table, right-aligned.
\captionsetup[table]{labelfont=normalfont, textfont=normalfont, labelsep=endash,
  justification=raggedleft, singlelinecheck=false, position=top, skip=6pt}

% Table content: 11 pt, single spacing, ragged-right cells.
\newcommand{\tablesetup}{\fontsize{11pt}{13pt}\selectfont\singlespacing\renewcommand{\arraystretch}{1.15}}
\newcolumntype{L}{>{\raggedright\arraybackslash}X}
\newcolumntype{P}[1]{>{\raggedright\arraybackslash}p{#1}}

% --- Lists: dash, lowercase items ending with ";" and a final "." (guidelines) ---
\setlist[itemize]{label=--, nosep, labelindent=\parindent, leftmargin=*, align=left}

% --- Source code: Courier 10 pt, single spacing ---
\lstdefinelanguage{TypeScript}{
  keywords={async, await, const, let, return, export, function, new, for, if, else,
            of, Promise, string, true, false},
  sensitive=true, comment=[l]{//}, morecomment=[s]{/*}{*/},
  morestring=[b]', morestring=[b]"}
\lstset{
  basicstyle=\fontsize{10pt}{11pt}\selectfont\ttfamily\singlespacing,
  keywordstyle=\bfseries,
  breaklines=true,
  showstringspaces=false,
  frame=single,
  framerule=0.5pt,
  rulecolor=\color{black},
  numbers=none,
  tabsize=2,
  aboveskip=6pt,
  belowskip=6pt
}

% Listings are numbered within the chapter and captioned like figures.
\renewcommand{\lstlistingname}{Listing}
\AtBeginDocument{\numberwithin{lstlisting}{section}}
\captionsetup[lstlisting]{labelfont=bf, textfont=bf, labelsep=endash,
  justification=centering, singlelinecheck=false}

% --- Keeping lead-in text with its table or figure ---
% The guidelines forbid a page that starts with a table or figure and require the
% text that cites it to precede it. The citing paragraph is therefore typeset inside
% the non-floating [H] block: if the block does not fit, the paragraph moves to the
% next page together with the table or figure.
\newcommand{\leadin}[1]{%
  {\onehalfspacing\setlength{\parindent}{1.25cm}\indent #1\par}\vspace{8pt}}

% --- Figures from TikZ sources in diagrams/ ---
% \diagram{file}{caption}{label}{lead-in paragraph}
\newcommand{\diagram}[4]{%
  \begin{figure}[H]
    \leadin{#4}
    \centering
    \adjustbox{max width=\textwidth}{\input{diagrams/#1.tex}}
    \caption{#2}
    \label{#3}
  \end{figure}}

% \screenshot{filepath}{caption}{label}{lead-in}{placeholder title}{placeholder instructions}
\newcommand{\screenshot}[6]{%
  \begin{figure}[H]
    \leadin{#4}
    \centering
    \IfFileExists{#1}{%
      \includegraphics[width=0.66\textwidth,keepaspectratio]{#1}%
    }{%
      \setlength{\fboxsep}{14pt}%
      \fbox{\parbox{0.82\textwidth}{\centering
        \vspace{8pt}
        {\bfseries\large [#5]}\par
        \vspace{6pt}
        {\small\textbf{Drop-in file location:} \nolinkurl{#1}}\par
        \vspace{8pt}
        {\footnotesize\itshape #6\par}
        \vspace{8pt}
      }}%
    }
    \caption{#2}
    \label{#3}
  \end{figure}}

% --- Appendix heading: "APPENDIX A" with the title centred beneath it ---
\newcommand{\startappendix}[3]{%
  \clearpage
  \phantomsection
  \addcontentsline{toc}{section}{APPENDIX #1 -- #2}
  \begin{center}
    {\fontsize{13pt}{16pt}\bfseries APPENDIX #1\par}
    \vspace{4pt}
    {\fontsize{13pt}{16pt}\bfseries #2\par}
  \end{center}
  \vspace{14pt}
  \setcounter{figure}{0}\setcounter{table}{0}\setcounter{lstlisting}{0}
  \renewcommand{\thefigure}{#1.\arabic{figure}}
  \renewcommand{\thetable}{#1.\arabic{table}}
  \renewcommand{\thelstlisting}{#1.\arabic{lstlisting}}}

% --- Bibliography: the heading comes from \unnumberedsection ---
\patchcmd{\thebibliography}{\section*{\refname}}{}{}{}

\begin{document}

% ==============================================================================
% TITLE PAGE (counted as page 1, number not displayed)
% ==============================================================================
\begin{titlepage}
\begin{center}
\begin{tabular*}{\textwidth}{@{\extracolsep{\fill}} c m{0.78\textwidth} @{}}
  \includegraphics[width=2.5cm,height=2.2cm,keepaspectratio]{images/media/image1.jpeg} &
  \centering
  {\fontsize{12pt}{14pt}\selectfont\bfseries TECHNICAL UNIVERSITY OF MOLDOVA}\\[0.15cm]
  {\fontsize{11pt}{13pt}\selectfont\bfseries Faculty of Computers, Informatics, and Microelectronics}\\[0.15cm]
  {\fontsize{11pt}{13pt}\selectfont\bfseries Department of Software Engineering and Automation}\\[0.15cm]
  {\fontsize{11pt}{13pt}\selectfont\bfseries Software Engineering Study Programme}
\end{tabular*}

\vspace{2.2cm}

{\fontsize{14pt}{16pt}\selectfont\bfseries INTERNSHIP REPORT}\\[0.4cm]
{\fontsize{13pt}{16pt}\selectfont\bfseries SECRETSHARE: ZERO-TRUST EPHEMERAL SECRET SHARING PLATFORM WITH END-TO-END HYBRID ENCRYPTION}

\vspace{2.2cm}

\begin{flushleft}
{\bfseries Team members:}\\[0.3cm]
{\fontsize{11pt}{13pt}\selectfont\singlespacing\renewcommand{\arraystretch}{1.35}%
\begin{tabularx}{\textwidth}{|>{\bfseries}p{0.30\textwidth}|>{\raggedright\arraybackslash}X|p{0.22\textwidth}|}
\hline
Student 1: & Cimendur Rustem, FAF-243 & * \\ \hline
Student 2: & Covali Ilie, FAF-243 & * \\ \hline
Student 3: & Kushnirenko Ecaterina, FAF-243 & * \\ \hline
Student 4: & Poiata Calin, FAF-243 & * \\ \hline
Student 5: & \c{S}oimu Ionu\c{t}, FAF-243 & * \\ \hline
University Internship Mentor & as. univ. Grosu Olga & * \\ \hline
\end{tabularx}}

\vspace{0.5cm}
{\fontsize{10pt}{12pt}\selectfont\itshape
*By signing the cover page of this report, each student confirms their active involvement in both the development of the project and the writing of the report. Students who did not contribute meaningfully are not permitted to sign.\par}
\end{flushleft}

\vfill

\vspace{0.3cm}
{\fontsize{11pt}{13pt}\selectfont Submission date: \underline{\hspace{1.5cm}} / \underline{\hspace{1.5cm}} / \underline{\hspace{2.0cm}}}\\[0.2cm]
{\fontsize{12pt}{14pt}\selectfont\bfseries Chisinau, 2026}
\end{center}
\end{titlepage}
% The titlepage environment resets the counter to 1; the next page is page 2.
\setcounter{page}{2}

% ==============================================================================
% PRELIMINARY PAGES (counted, page numbers not displayed)
% ==============================================================================
\pagestyle{empty}

\plainheading{Table of Contents}
\tableofcontents

% ------------------------------------------------------------------------------
\plainheading{Abstract}

This report presents SecretShare, a web application for sending a confidential value, such as a database password or an API key, to one named colleague so that the value can be read exactly once and is then destroyed. The problem addressed is that credentials exchanged in chat, e-mail and issue trackers remain stored in those systems indefinitely, while existing one-time-link services either encrypt on the server or let anyone who obtains the link read the secret.

The proposed solution encrypts every secret in the sender's browser with AES-256-GCM and wraps the content key with RSA-OAEP for the devices that the recipient has registered. The server stores only the resulting envelope in Redis with a time to live and never receives the plaintext or an unwrapped key. Retrieval requires the recipient to sign in through Keycloak with OpenID Connect and PKCE, and a Lua script executed inside Redis checks the caller and deletes the envelope in one atomic step, so a wrong recipient cannot destroy the secret and two readers cannot both obtain it. Secret identifiers are sent only in request bodies, which keeps them out of the request lines recorded by proxies and web servers.

The minimum viable product was built by a team of five students during a four-week internship with a FastAPI back end, a Next.js front end, PostgreSQL for device keys and an append-only audit log, and Traefik for routing. Methods included threat analysis, UML modelling, test-driven fixes of discovered vulnerabilities and continuous integration with a preview environment for every pull request. The latest pipeline run executed 176 automated API and security tests, 18 internal-TLS tests and 5 live OAuth integration tests without failures. The report also records the limitations that remain, including multi-factor authentication, a restricted database role for the audit log and protection against losing a secret on an unregistered device, as the plan for the continuation of the project in the PBL course.

\vspace{0.6cm}
\noindent
\textbf{Keywords:} ephemeral secret sharing, end-to-end encryption, hybrid cryptography, zero-knowledge storage, OpenID Connect, Redis, secure software development.

% ------------------------------------------------------------------------------
\clearpage
\phantomsection
\addcontentsline{toc}{section}{LIST OF ABBREVIATIONS}
\begin{center}
  {\fontsize{13pt}{16pt}\bfseries LIST OF ABBREVIATIONS\par}
\end{center}
\vspace{12pt}
{\fontsize{11pt}{13.5pt}\selectfont\singlespacing
\noindent
\begin{tabular}{@{}P{0.10\textwidth} P{0.36\textwidth} @{\hspace{0.02\textwidth}} P{0.10\textwidth} P{0.36\textwidth}@{}}
\textbf{AES} & Advanced Encryption Standard & \textbf{OIDC} & OpenID Connect \\
\textbf{API} & Application Programming Interface & \textbf{OWASP} & Open Worldwide Application Security Project \\
\textbf{CA} & Certificate Authority & \textbf{PBL} & Project-Based Learning \\
\textbf{CSP} & Content Security Policy & \textbf{PDF} & Portable Document Format \\
\textbf{CSPRNG} & Cryptographically Secure Pseudo-Random Number Generator & \textbf{PEM} & Privacy-Enhanced Mail (text encoding of keys) \\
\textbf{DAS} & Development of Secure Applications & \textbf{PKCE} & Proof Key for Code Exchange \\
\textbf{FR} & Functional Requirement & \textbf{PR} & Pull Request \\
\textbf{GCM} & Galois/Counter Mode & \textbf{REST} & Representational State Transfer \\
\textbf{GDPR} & General Data Protection Regulation & \textbf{RS256} & RSA signature with SHA-256 \\
\textbf{HTTP} & Hypertext Transfer Protocol & \textbf{RSA} & Rivest--Shamir--Adleman \\
\textbf{HTTPS} & HTTP over TLS & \textbf{SHA} & Secure Hash Algorithm \\
\textbf{ID} & Identifier & \textbf{SPKI} & Subject Public Key Info \\
\textbf{IP} & Internet Protocol & \textbf{SRE} & Site Reliability Engineering \\
\textbf{IV} & Initialisation Vector & \textbf{SSH} & Secure Shell \\
\textbf{JSON} & JavaScript Object Notation & \textbf{TLS} & Transport Layer Security \\
\textbf{JWKS} & JSON Web Key Set & \textbf{TTL} & Time To Live \\
\textbf{JWT} & JSON Web Token & \textbf{UI} & User Interface \\
\textbf{KB} & Kilobyte & \textbf{UML} & Unified Modeling Language \\
\textbf{NFR} & Non-Functional Requirement & \textbf{URL} & Uniform Resource Locator \\
\textbf{OAEP} & Optimal Asymmetric Encryption Padding &  &  \\
\end{tabular}
}

% ==============================================================================
% MAIN BODY (page numbers displayed from the Introduction onwards)
% ==============================================================================
\clearpage
\pagestyle{fancy}

\unnumberedsection{INTRODUCTION}

Software teams exchange credentials every day: a developer needs a database password during onboarding, an operator shares a temporary cloud token during an incident, and a contractor receives an API key for an integration. In practice these values are usually pasted into the tools the team already uses. The GitGuardian report on secrets sprawl found leaked secrets in 2.4\% of the corporate Slack channels and 6.1\% of the Jira tickets that it analysed \cite{gitguardian2025}. Such messages are kept for years, copied into backups and indexed for search, so one compromised account exposes a history of credentials. Stolen credentials matter because credential abuse remains the leading initial access vector \cite{verizon2025}, as evidenced by the 2022 Uber incident \cite{uber2022} and Operation Emerald Whale \cite{emeraldwhale2024}.

One-time secret links were created to shorten this exposure: a secret is stored temporarily and deleted after the first read. However, the common designs leave three weaknesses. When encryption happens on the server, the operator of the service can read every secret. When the link itself is the only credential, anybody who sees the link, including a chat application that fetches it to build a preview, can consume the secret. When the identifier is part of the URL path or query, it is written into the access logs of every proxy and web server on the way. These weaknesses are analysed in detail in Chapter~1.

The project was carried out as part of the Development of Secure Applications (DAS) internship, whose brief asks teams to build a realistic minimum viable product in which security is designed in from the start: encryption of data in transit and at rest, OAuth~2.0 for API access, input validation, error handling that does not disclose information, and documentation of threats, risks and incident response \cite{dasbrief}. A one-time secret relay suits this brief well because every one of its functions is security-relevant, and because its correctness can be expressed as a small number of verifiable invariants.

The purpose of the project is to design, implement, and verify SecretShare, a relay in which the server can neither read a secret nor deliver it to anyone except the intended recipient. To achieve this, key engineering objectives were executed: domain analysis and threat modeling against real-world breach vectors; formal specification of functional requirements and measurable security invariants; structural and behavioral modeling using standard UML diagrams; implementation of browser-side hybrid encryption, recipient authentication, and atomic Lua single delivery in Redis; and systematic security verification via automated test suites and continuous delivery workflows. For engineering teams, the resulting operational impact is that shared credentials persist ephemerally in encrypted memory rather than permanently in searchable chat logs.

The work was done by a team of five students during the September 2026 internship; the repository was created on 9 September. Changes went through pull requests on GitHub, and by the time of writing 28 pull requests had been merged, each of them tested automatically and, from the middle of the month, deployed to its own preview environment \cite{secretsharerepo}.

Chapter~1 of the report analyses the domain and the market, Chapter~2 presents the requirements, the UML models and the cryptographic design, and Chapter~3 describes the implementation, the security controls, the testing and the problems solved during the internship.

% ==============================================================================
\clearpage
\section{Domain Analysis}

This chapter defines the problem that SecretShare addresses and the attackers that the design must withstand. It then describes the intended users and their needs, the concept of the solution and the scope chosen for the first version, and compares the concept with existing tools.

\subsec{Problem Overview}

A credential shared between people has two properties that make it dangerous: it grants access by itself, and it is usually valid for much longer than the conversation in which it was sent. General-purpose communication tools are built to preserve messages. A password typed into a chat is stored on the provider's servers, synchronised to every device of every participant, included in exports and backups, and indexed for search. Deleting the message later rarely removes all of these copies. The collaboration tools that engineering teams use most, such as Slack, Jira and Confluence, are therefore a recurring source of leaked secrets \cite{gitguardian2025}.

One-time secret services reduce the lifetime of the copy, but an analysis of their typical design shows four weaknesses that SecretShare is intended to remove.

The first weakness is trust in the server. If the plaintext is sent to the server and encrypted there, the confidentiality of every secret depends on the honesty and security of the operator, of the hosting provider and of every administrator with access to the machine. A single compromise of the server exposes every secret that passes through it while the attacker is present.

The second weakness is the link as the only credential. A link that is sufficient to read the secret can be used by anyone who sees it: a colleague who reads over the recipient's shoulder, a clipboard manager that stores history, or a mistaken paste into the wrong channel. The service cannot tell the intended recipient from any other holder of the link.

The third weakness concerns request lines. HTTP/1.1 sends the method and the request target, that is the path and the query string, in the first line of every request \cite{rfc9112}. Web servers and reverse proxies record this line in their access logs by default, and browsers store it in their history. A secret identifier placed in the path or the query therefore ends up in log files that live far longer than the secret itself, and anyone with read access to those logs can use an unconsumed identifier. Only the fragment part of a URL, after the \texttt{\#} sign, is processed by the client and not sent to the server \cite{rfc3986}.

The fourth weakness is automatic consumption. Chat applications fetch a pasted link to build a preview \cite{slackunfurl}, and security gateways scan links in incoming mail. If opening the link is enough to consume the secret, such an automated request destroys it before the person for whom it was intended opens it, or worse, a scanner that stores responses keeps a copy.

To reason about these weaknesses systematically, the team defined six threat actors, from a reader of log files to a compromised server.

\begin{table}[H]
\leadin{As shown in Table~1.1, each threat actor is paired with the protection that the design provides and with the risk that remains; the remaining risks are revisited in the Conclusions.}
\tablesetup
\caption{Threat actors and the corresponding protections}
\label{tab:threats}
\begin{tabularx}{\textwidth}{|P{0.20\textwidth}|P{0.25\textwidth}|L|P{0.19\textwidth}|}
\hline
\textbf{Actor} & \textbf{Capability} & \textbf{Protection in SecretShare} & \textbf{Residual risk} \\ \hline
Log reader & reads proxy, server or audit logs & identifiers only in request bodies; audit stores an 8-character prefix & logs written before the fix \\ \hline
Link holder & obtains a share link & reveal requires the recipient's signed-in identity & a holder learns that a secret exists \\ \hline
Link previewer & fetches every pasted link & opening a link only runs a non-destructive existence check & none identified \\ \hline
Curious or compromised server & reads Redis, PostgreSQL and memory & server stores ciphertext and wrapped keys only & a server that serves malicious JavaScript \\ \hline
Network attacker & observes traffic between containers & TLS with a CA-verified certificate to Redis and PostgreSQL & host names are not verified \\ \hline
Wrong recipient & signs in and calls reveal & Lua script refuses and keeps the secret & learns that the secret is not theirs \\ \hline
\end{tabularx}
\end{table}

The table shows that the most serious remaining risk is the server that delivers the JavaScript code: an attacker who controls it can send a script that reads the plaintext before it is encrypted. This risk is inherent to every browser-based end-to-end encryption scheme; it is reduced, but not removed, by the Content Security Policy described in Chapter~3.

The necessity of eliminating static credential sprawl is demonstrated by notable real-world breaches where initial access escalated into catastrophic infrastructure compromise. In the September 2022 Uber breach, an attacker obtained an external contractor's credentials through social engineering and multi-factor authentication (MFA) fatigue \cite{uber2022}. Once inside the intranet, the intruder performed reconnaissance and discovered plaintext administrative credentials for a Privileged Access Management (PAM) solution embedded in network shares and internal scripts, enabling full lateral takeover of cloud and identity systems. Similarly, in mid-2024, the Sysdig Threat Research Team uncovered Operation Emerald Whale, an automated cyber campaign that exfiltrated over 15,000 Git configurations, private repositories, and cloud tokens sprawled across developer systems \cite{emeraldwhale2024}. SecretShare directly remedies the root causes of both incidents: by enforcing browser-side hybrid encryption, recipient authentication, and automatic single-delivery destruction (burn-after-reading), credentials never persist in corporate chat histories, file shares, or server databases, depriving intruders of static credential backlogs even following an endpoint compromise.

% ------------------------------------------------------------------------------
\subsec{Target Audience}

SecretShare is intended for organisations in which people regularly hand credentials to each other and which already operate a central identity provider. The needs of the target users were derived from the team's experience, discussions with the mentor and the DAS brief \cite{dasbrief}; no formal survey was carried out, which is a gap to be closed in the PBL semester. Four groups of users were identified.

\begin{table}[H]
\leadin{As shown in Table~1.2, each group has specific needs, and every need translates into a product feature that is specified as a requirement in Chapter~2.}
\tablesetup
\caption{User needs and the features that address them}
\label{tab:needs}
\begin{tabularx}{\textwidth}{|P{0.24\textwidth}|L|P{0.30\textwidth}|}
\hline
\textbf{User group} & \textbf{Need} & \textbf{Feature} \\ \hline
Developers & send a secret quickly from any browser & web interface, share link with copy button \\ \hline
Developers & no manual key management & device keys generated and registered on first sign-in \\ \hline
DevOps and SRE & secret must not outlive the handover & TTL from 5 minutes to 24 hours, burn on read \\ \hline
DevOps and SRE & integrate with scripts & documented REST API with OpenAPI description \\ \hline
Security officers & evidence of reading & append-only audit log of creation, reading and denial \\ \hline
Security officers & no new store of secrets & ciphertext only; audit keeps an identifier prefix \\ \hline
External contractors & receive a secret without a separate tool & addressing by identity-provider user name \\ \hline
\end{tabularx}
\end{table}

Developers form the largest group. They receive database passwords, API keys and test accounts when they join a project or start a new integration, and they send such values to colleagues who work on other parts of the system. Their main needs are speed and simplicity: a secret must be sent in less than a minute, from any operating system, without installing software or managing encryption keys by hand.

DevOps engineers and site reliability engineers provision infrastructure and rotate credentials. They often need to hand over a temporary token during an incident, under time pressure and in a channel that other people can see. For them it is essential that a secret expires automatically if it is not read, that its size is limited, and that the handover works through an interface that they can also call from scripts.

Security officers and auditors do not send secrets themselves, but they are responsible for policies that forbid credentials in chat. They need a sanctioned alternative that users will actually adopt, and evidence after the fact: who created a secret, whether and when it was read, and whether someone else tried to read it. At the same time the evidence must not become a new store of secrets.

External contractors and partners are the fourth group. They are often given accounts in the organisation's identity provider for the duration of a contract. The system must allow them to receive a secret addressed to their account, and must make sure that a secret sent to a contractor who never opens it disappears on its own. Anonymous recipients are not served, as that would reintroduce the link-holder weakness.


% ------------------------------------------------------------------------------
\subsec{Solution Concept}

SecretShare is a relay for secrets addressed to a named person. A signed-in sender types the secret and the user name of the recipient. The browser encrypts the secret for the devices that the recipient has registered, sends the encrypted envelope to the server and receives a share link. The recipient opens the link, sees a warning that the secret will be destroyed, signs in if necessary and confirms. The server hands the envelope to the recipient and deletes it in the same step, and the recipient's browser decrypts it locally. The concept rests on four principles.

The first principle is that the server is blind. Encryption and decryption use the Web Cryptography API of the browser \cite{webcrypto}, and the server receives only ciphertext, an initialisation vector and content keys wrapped with the recipients' public keys. The server judges the envelope only by its size and by the validity of its fields.

The second principle is that access is bound to identity, not to the link. Reading a secret requires a valid access token issued by the organisation's identity provider, and the server hands out the envelope only if the user in the token is the recipient chosen by the sender. The link merely identifies which secret to fetch.

The third principle is that a secret is delivered once. The check of the recipient and the deletion happen as one atomic operation inside the data store, so neither a wrong recipient nor two parallel requests of the right recipient can break the guarantee.

The fourth principle is that nothing secret is logged. Identifiers travel in request bodies, never in the path or query of an API request, error responses do not echo input, and the audit log stores only a short prefix of an identifier.

\begin{table}[H]
\leadin{As shown in Table~1.3, a secret passes through a small number of states, and every state has a defined location and a defined protection.}
\tablesetup
\caption{Life cycle of a secret}
\label{tab:lifecycle}
\begin{tabularx}{\textwidth}{|P{0.14\textwidth}|P{0.21\textwidth}|L|P{0.24\textwidth}|}
\hline
\textbf{State} & \textbf{Location} & \textbf{Protection} & \textbf{Leaves the state when} \\ \hline
Composed & sender's browser memory & never sent in plaintext & the sender submits the form \\ \hline
Stored & Redis, key \texttt{s:<id>} & AES-256-GCM ciphertext, key wrapped per device, TTL set & recipient reveals it or the TTL elapses \\ \hline
Burned & deleted & Lua script deletes it atomically after the recipient check & final state \\ \hline
Expired & deleted by Redis & no copy remains on the server & final state \\ \hline
\end{tabularx}
\end{table}

A request by a user who is not the recipient, and an existence check made when the link is opened, leave the secret in the stored state. This distinction is what protects the secret from link previews and from a wrong recipient.

Following the DAS brief \cite{dasbrief}, the team implemented in September the measures without which the concept does not hold: browser-side encryption, OAuth~2.0 and OpenID Connect authentication, atomic delivery, identifiers kept out of logs, internal TLS, validation and rate limiting. Multi-factor authentication and a sender dashboard were planned for October to December.

% ------------------------------------------------------------------------------
\subsec{Market Research}

The concept was evaluated against four representative tools documented in primary literature.

HashiCorp Vault provides response wrapping to encapsulate secrets behind single-use, time-limited tokens \cite{vaultwrap}. While cryptographically robust, it requires both parties to share Vault infrastructure, creating excessive operational overhead for ad-hoc credential exchanges between individuals.

Bitwarden Send provides client-side encryption with configurable access counts and optional passwords \cite{bitwardensend}. However, because recipients require no identity enrollment, the retrieval link itself functions as the sole credential unless secondary verification channels are configured.

Yopass encrypts payloads in-browser using OpenPGP with the decryption key stored in the URL fragment \cite{yopass}. While server-blind, the open-source version lacks user identity gating, releasing secrets to any agent presenting the link.

Onetime Secret executes encryption server-side by default and deletes data upon initial display \cite{onetimesecret}, exposing unencrypted credentials to hosting infrastructure and access log inspection.

\begin{table}[H]
\leadin{As shown in Table~1.4, each existing tool covers part of the problem, but none of the open-source tools combines browser-side encryption with a check of the recipient's identity before a secret is released.}
\tablesetup
\caption{Comparison of secret-sharing tools}
\label{tab:market}
\begin{tabularx}{\textwidth}{|L|P{0.12\textwidth}|P{0.13\textwidth}|P{0.12\textwidth}|P{0.14\textwidth}|P{0.13\textwidth}|}
\hline
\textbf{Property} & \textbf{Vault} & \textbf{Bitwarden Send} & \textbf{Yopass} & \textbf{Onetime Secret} & \textbf{SecretShare} \\ \hline
Encryption in the browser & no (server) & yes & yes & no (server) & yes \\ \hline
Server can decrypt & yes & no & no & without passphrase & no \\ \hline
Recipient identity checked & no, token holder & optional e-mail & commercial edition & no & yes, OIDC \\ \hline
Single use & yes & access limit & optional & yes & yes, atomic \\ \hline
Denied reader keeps secret & -- & -- & -- & -- & yes \\ \hline
Open source, self-hosted & yes & yes & yes & yes & yes \\ \hline
Needs recipient enrolment & yes & no & no & no & yes \\ \hline
\end{tabularx}
\end{table}

The comparison also shows the price of the approach: the recipient must have signed in once before a secret can be sent to them, and the organisation must operate an identity provider. SecretShare therefore targets the internal handover of credentials between known people; for sharing with the public, tools such as Yopass remain simpler.

% ==============================================================================
\clearpage
\section{System Design}

This chapter turns the concept into a specification. It lists the functional and non-functional requirements, models the behaviour of the system with a use case diagram and sequence diagrams, describes its structure with a component diagram and the data model, and specifies the cryptographic scheme together with the invariants that the implementation must preserve.

\subsec{Technical Requirements}

The requirements were derived from the user needs in Table~1.2 and from the threat actors in Table~1.1. Each requirement received an identifier so that it can be traced to the code and to the tests that verify it.

\begin{table}[H]
\leadin{As shown in Table~2.1, the functional requirements describe what the users can do and which part of the system realises each operation.}
\tablesetup
\caption{Functional requirements}
\label{tab:fr}
\begin{tabularx}{\textwidth}{|P{0.08\textwidth}|L|P{0.29\textwidth}|}
\hline
\textbf{ID} & \textbf{Requirement} & \textbf{Realised by} \\ \hline
FR-01 & A user signs in through the organisation's identity provider using the authorisation code flow with PKCE. & Keycloak, Auth.js \\ \hline
FR-02 & On first sign-in the browser generates an RSA key pair, keeps the private key locally and registers the public key. & \texttt{POST /api/keys/register} \\ \hline
FR-03 & A sender obtains the public keys of the recipient's active devices. & \texttt{GET /api/keys/\{user\_id\}} \\ \hline
FR-04 & The browser encrypts the secret and sends only the envelope, with an optional lifetime of 5 minutes to 24 hours. & \texttt{POST /api/secrets} \\ \hline
FR-05 & The sender receives an identifier and a share link that can be copied with one click. & web interface \\ \hline
FR-06 & Opening a link checks whether the secret exists without consuming it and shows a burn warning. & \texttt{POST /api/secrets/exists} \\ \hline
FR-07 & After confirmation, the signed-in recipient receives the envelope, which is deleted at the same moment. & \texttt{POST /api/secrets/reveal} \\ \hline
FR-08 & The recipient's browser decrypts the secret in memory. & \texttt{lib/crypto.ts} \\ \hline
FR-09 & Creation, reading and denied reading attempts are recorded in an audit log. & \texttt{audit\_events} table \\ \hline
\end{tabularx}
\end{table}

The non-functional requirements define the quality attributes, most of which are security properties. They are formulated so that each of them can be checked by an automated test.

\begin{table}[H]
\leadin{As shown in Table~2.2, several non-functional requirements are stated as invariants: properties that must hold for every request, not only on average.}
\tablesetup
\caption{Non-functional requirements}
\label{tab:nfr}
\begin{tabularx}{\textwidth}{|P{0.08\textwidth}|P{0.17\textwidth}|L|}
\hline
\textbf{ID} & \textbf{Attribute} & \textbf{Requirement} \\ \hline
NFR-01 & confidentiality & The server never receives plaintext or an unwrapped content key and judges an envelope only by size and field validity. \\ \hline
NFR-02 & single delivery & Two concurrent reveal requests never both receive the same secret. \\ \hline
NFR-03 & integrity of access & A request by anyone except the recipient never deletes the secret. \\ \hline
NFR-04 & uniform errors &Unknown, burned and expired identifiers produce identical status, body and headers. \\ \hline
NFR-05 & no secret logging & No full identifier, ciphertext or token appears in any log; identifiers never appear in API paths or query strings. \\ \hline
NFR-06 & identifier strength & Identifiers carry 256 bits from a CSPRNG. \\ \hline
NFR-07 & resource limits & The request body is limited to 64 KB; clients are limited to 10 creations and 30 retrieval calls per minute. \\ \hline
NFR-08 & transport security & Traffic from the API to Redis and PostgreSQL uses TLS with certificates issued by an internal CA. \\ \hline
NFR-09 & availability & A failure of the audit database does not change any response to a secret request. \\ \hline
NFR-10 & usability & The system works in a current browser without installing software. \\ \hline
\end{tabularx}
\end{table}

The numeric limits in NFR-07 and the lifetime bounds in FR-04 are configuration values rather than constants in the code; they are read from the environment at start-up and validated, so an invalid combination, such as a default lifetime outside the allowed range, stops the service instead of being accepted silently.

% ------------------------------------------------------------------------------
\subsec{Behavioural Modelling}

The behaviour of the system is modelled first by its use cases, which show who interacts with SecretShare and for what purpose.

\diagram{usecase}{Use case diagram of SecretShare}{fig:usecase}{According to Figure~2.1, the system has two human actors and two external actors.}

The sender signs in and creates secrets; creating a secret includes looking up the recipient's device keys. The recipient signs in, opens share links and reveals secrets; opening a link includes the existence check, and revealing includes the local decryption. Signing in includes the registration of the device key. Keycloak takes part in every sign-in, and the expiry mechanism of Redis acts as a timer that removes unread secrets.

The two central use cases are described in more detail by sequence diagrams.

\diagram{seq_create}{Sequence diagram of secret creation}{fig:seqcreate}{According to Figure~2.2, the creation of a secret starts with a request for the recipient's public keys.}

The browser then generates a fresh content key and initialisation vector, encrypts the plaintext and wraps the content key once for each device. The API checks the size of the request and the requested lifetime, generates an identifier, stores the envelope in Redis with the lifetime as its expiry, writes an audit event and returns the identifier together with the expiry time.

\diagram{seq_reveal}{Sequence diagram of secret retrieval}{fig:seqreveal}{According to Figure~2.3, the retrieval of a secret is divided into two phases.}

In the first phase the browser only asks whether the secret exists. This request is safe for link previews and for accidental clicks, because it never changes the state of the secret; its positive answer makes the interface show a warning that reading will destroy the secret. The second phase starts only when the person confirms. If the person is not signed in, the authorisation code flow is carried out first. The reveal request carries the identifier in the body and the access token in the header. The API verifies the token, takes the user name from it and runs the burn script, which returns one of three results: the envelope, a denial or a miss. The audit event and the HTTP status follow from this result, and on success the browser decrypts the secret locally.

The registration of a device key, which precedes both flows, is shown in Appendix~A. It is performed automatically after the first sign-in in a browser, so the user never handles keys directly.

% ------------------------------------------------------------------------------
\subsec{Structural Modelling}

The structure of the system is described by a component diagram, which shows the deployable parts, their internal modules and the interfaces between them.

\diagram{component}{Component diagram of SecretShare}{fig:component}{According to Figure~2.4, SecretShare consists of five runtime components behind a single edge router, plus a start-up component that prepares certificates; all of them run as containers started together by Docker Compose.}

Traefik is the only component exposed to the network \cite{traefik}. It routes requests by path prefix: \texttt{/api} goes to the FastAPI service, except \texttt{/api/auth}, which Auth.js serves from the web application, \texttt{/keycloak} goes to the identity provider and all other paths to the Next.js web application. In the local configuration Traefik also publishes its dashboard on port 8080 without authentication, and the Keycloak administration console is reachable under \texttt{/keycloak/admin}; both are limitations recorded in Section~3.3. Because every component is reached through the same origin, the browser's same-origin policy and the Content Security Policy can be as strict as \texttt{'self'}, and no cross-origin resource sharing is needed. In preview environments Tailscale Serve terminates HTTPS in front of Traefik and gives each environment its own host name with a valid certificate \cite{tailscaleserve}; the staging server runs the local configuration over plain HTTP. A secure context is not optional here: the Web Cryptography API is available only on HTTPS pages or on \texttt{localhost}.

The web component is a Next.js application \cite{nextjs} that serves the user interface and uses Auth.js to run the OpenID Connect sign-in with Keycloak \cite{authjs}. All cryptographic work runs in the user's browser, in the modules shown in the upper part of Figure~2.4.

The API component is a FastAPI application \cite{fastapi} organised in layers. Middleware limits the body size, determines the real client address behind trusted proxies and adds security headers. Route handlers validate input with Pydantic schemas and apply rate limits. Services implement the use cases, the \texttt{SecretStore} class wraps Redis and the Lua burn script, and the \texttt{AuditService} writes audit events. Authentication is a dependency that verifies access tokens against the signing keys that Keycloak publishes as a JSON Web Key Set.

Keycloak is the identity provider \cite{keycloak}. It holds the user accounts, performs authentication and issues signed JSON Web Tokens \cite{rfc7519} for the public client \texttt{secretshare-api}.

Redis holds the encrypted envelopes, each under the key \texttt{s:<payload\_id>} with an expiry equal to the lifetime of the secret. Redis is used only as volatile storage, so an unread secret disappears without any clean-up job.

PostgreSQL holds the data that must persist: the users known to the relay, their device public keys and the audit log. The data model is shown in Appendix~B. It contains three tables. The \texttt{users} table identifies a person by a platform and a platform-specific user name, which prepares the model for chat platforms. The \texttt{device\_keys} table stores public keys only, with a \texttt{revoked\_at} column that marks keys that are no longer active. The \texttt{audit\_events} table is append-only and is described in Chapter~3. The same PostgreSQL server also hosts the Keycloak database, and both are accessed with one database account.

The \texttt{tls-init} component runs once at start-up. It creates a private certificate authority and issues server certificates for PostgreSQL and Redis. The API connects to both data stores over TLS and verifies their certificates against this authority; Redis listens only on its TLS port, with the plaintext port disabled. The TLS is not mutual: the data stores do not ask the API for a client certificate. PostgreSQL authenticates the API with a password, whereas Redis currently accepts any TLS client on the internal network without authentication, which is recorded as a limitation in Section~3.3.

% ------------------------------------------------------------------------------
\subsec{Cryptographic Design and Security Invariants}

The cryptographic scheme is a hybrid one: a symmetric cipher protects the secret and an asymmetric cipher protects the symmetric key for each of the recipient's devices. This combination allows a secret of any length up to the size limit to be encrypted once and addressed to several devices, while every device keeps its own private key.

For the content, the browser draws a 256-bit key $K$ and a 96-bit initialisation vector from its cryptographically secure random number generator and encrypts the plaintext with AES in Galois/Counter Mode \cite{nist80038d}. GCM is an authenticated mode: the ciphertext carries a 128-bit tag, and any change to the ciphertext or the initialisation vector makes decryption fail instead of producing altered plaintext. A new key and initialisation vector are used for every secret, which satisfies the requirement of GCM that an initialisation vector is never reused with the same key.

For the key, each device owns an RSA key pair with a 2048-bit modulus, generated in the browser. The content key is wrapped with RSA-OAEP using SHA-256 \cite{rfc8017}. According to formula (2.1), the wrapped key for device $j$ is computed as follows:
\begin{equation}
W_j = \mathrm{RSA\text{-}OAEP}_{\mathrm{SHA\text{-}256}}\left(PK_j, K\right),
\label{eq:wrap}
\end{equation}
where $W_j$ is the content key wrapped for device $j$; $PK_j$ is the public key of device $j$, registered in SPKI format; $K$ is the 256-bit content key of the secret.

According to formula (2.2), the envelope that the browser sends and the server stores is the following tuple:
\begin{equation}
E = \left(r,\ \{(d_j, W_j)\}_{j=1}^{n},\ IV,\ C\right),
\label{eq:envelope}
\end{equation}
where $E$ is the envelope stored in Redis; $r$ is the user name of the recipient; $d_j$ is the identifier of the $j$-th device of the recipient; $W_j$ is the content key wrapped for that device according to formula (2.1); $n$ is the number of active devices of the recipient; $IV$ is the 96-bit initialisation vector; $C$ is the AES-GCM ciphertext including its authentication tag. None of these values allows the server to recover the plaintext without a private key, and the private keys never leave the browsers. The lifetime is not part of the envelope; it is stored by Redis as the expiry of the key.

The identifier of a secret is generated on the server by \texttt{secrets.token\_urlsafe(32)}, which encodes 32 bytes from the operating system's random source in 43 URL-safe characters. According to formula (2.3), the probability that an attacker guesses any of the stored identifiers in a series of attempts is bounded as follows:
\begin{equation}
P \le \frac{N \cdot q}{2^{256}},
\label{eq:guess}
\end{equation}
where $P$ is the probability of a successful guess; $N$ is the number of secrets stored at the same time; $q$ is the number of guesses; $2^{256}$ is the number of possible identifiers. Even with a million stored secrets and the rate limit of 30 attempts per minute sustained for a century, $P$ stays below $10^{-61}$, so guessing is not a practical attack.

Single delivery rests on the burn script in Listing~2.1. Redis executes a Lua script atomically, without running other commands in between \cite{redislua}, so reading the envelope, comparing the recipient and deleting the key form one indivisible step.

\noindent\begin{minipage}{\textwidth}
\begin{lstlisting}[language={[5.2]Lua}, caption={Atomic recipient-checked burn script}, label={lst:burn}, captionpos=b]
local payload = redis.call('GET', KEYS[1])
if not payload then
  return {'missing'}
end
local ok, envelope = pcall(cjson.decode, payload)
if not ok or type(envelope) ~= 'table' or envelope['recipient_id'] ~= ARGV[1] then
  return {'denied'}
end
redis.call('DEL', KEYS[1])
return {'burned', payload}
\end{lstlisting}
\end{minipage}

Two simpler designs were rejected. Reading and deleting with \texttt{GETDEL} before checking the recipient destroys the secret when the wrong person asks for it; this was the behaviour of an early version and is described in Chapter~3. Reading with \texttt{GET}, checking in Python and then deleting with \texttt{DEL} lets two parallel requests of the recipient both read the envelope before either deletes it.

The design is summarised by five invariants that the tests in Chapter~3 verify. The source code refers to them by the numbers of the team's internal specification, which are kept so that a reader can follow the references.

\begin{table}[H]
\leadin{As shown in Table~2.3, each invariant is enforced by a specific part of the code.}
\tablesetup
\caption{Security invariants and their enforcement}
\label{tab:invariants}
\begin{tabularx}{\textwidth}{|P{0.05\textwidth}|P{0.34\textwidth}|L|}
\hline
\textbf{No.} & \textbf{Invariant} & \textbf{Enforced by} \\ \hline
1 & the server is blind to envelope contents and judges only their size & Pydantic schemas, body size middleware \\ \hline
3 & two concurrent readers never both receive a secret & Lua burn script in \texttt{SecretStore} \\ \hline
4 & identifiers carry 256 bits from a CSPRNG & \texttt{core/ids.py} \\ \hline
5 & unknown, burned and expired identifiers are indistinguishable & one 404 handler, no separate expiry error, audit failures ignored \\ \hline
6 & nothing secret is logged & identifiers in bodies, log redaction filter, 8-character audit prefix \\ \hline
\end{tabularx}
\end{table}

Together with the requirements in Tables~2.1 and 2.2, these invariants form the acceptance criteria against which the implementation in Chapter~3 was tested.

% ==============================================================================
\clearpage
\section{Implementation and Security Verification}

This chapter describes how the design was implemented, which security controls protect the implementation, how the controls were tested and delivered, and which problems were discovered and solved during the internship. Each security control is presented in the form required by the DAS brief: the problem it addresses, its implementation, its testing and its limitations \cite{dasbrief}.

\subsec{Frontend Implementation and User Interface}

The SecretShare frontend is implemented as a single-page web application using Next.js 15 with the App Router, React 19, Auth.js (NextAuth v5), and TypeScript. To mitigate frontend supply-chain vulnerabilities, the client avoids third-party component suites and utility-first CSS tooling. Instead, the interface relies on semantic HTML and a custom, zero-dependency design system defined in \texttt{globals.css} using CSS custom properties with a Gruvbox palette, dark by default and light when the operating system requests a light colour scheme, and glassmorphism styling tokens. Sensitive client-side operations are isolated within Client Components designated with the \texttt{"use client"} directive, maintaining a strict boundary between server-rendered layouts and browser cryptographic memory.

User authentication is integrated through Keycloak via OpenID Connect with PKCE, managed by Auth.js route handlers under \texttt{web/auth.ts}. Because Traefik routes the frontend, the API under \texttt{/api}, and the Keycloak realm under \texttt{/keycloak} onto a single domain, authentication flows operate strictly within a unified same-origin scope. The active session extracts the authenticated user identity (\texttt{preferred\_username}) and bearer access token, scoping client cryptographic storage and authorized API calls without exposing credentials to untrusted origins.

Client-side cryptographic operations interface directly with the standard Web Cryptography API (\texttt{window.crypto.subtle}) in \texttt{lib/crypto.ts} without third-party mathematical libraries. On initial sign-in, the application verifies whether an RSA-OAEP 2048-bit key pair (using SHA-256 and public exponent 65537) exists for the active username. If absent, the browser generates a key pair, exports the public key in SubjectPublicKeyInfo (SPKI) PEM format, and registers it via \texttt{POST /api/keys/register}. Both keys are stored on the local device as \texttt{CryptoKey} objects in an IndexedDB datastore named \texttt{secretshare\_keystore} under the \texttt{device\_keys} object store. The key pair is currently generated with the extractable flag set, so a script running in the page could export the private key; generating it as non-extractable is planned for the PBL semester.

To enforce application security and prevent premature secret destruction, the user interface structures both secret creation and secret retrieval into two sequential, verifiable actions. For secret creation, the first action configures the recipient, payload, and lifetime, while the second action triggers hybrid encryption and presents the resulting ephemeral link.

\clearpage

\screenshot{images/screenshots/secret_create1.png}%
  {Secret composition form with recipient field and expiration selection}%
  {fig:secret_create_form}%
  {As shown in Figure~3.1, the secret composition interface provides input fields where the user enters the Keycloak user name of the recipient and the sensitive plaintext, and selects an expiration window between 5 minutes and 24 hours, while the status banner verifies that the local device key is registered for end-to-end encryption.}%
  {Figure 3.1: Secret Composition Interface}%
  {Secret composition form displaying the recipient user name field, secret input, expiration selection, and cryptographic readiness indicator.}

When the sender clicks the creation button, the browser fetches the recipient's registered public keys via \texttt{GET /api/keys/\{user\_id\}} and executes local hybrid encryption: it creates a random 256-bit AES-GCM content key, encrypts the plaintext with a fresh 96-bit initialization vector, wraps the key for each active recipient device, and transmits the resulting ciphertext envelope to \texttt{POST /api/secrets}.

\screenshot{images/screenshots/secret_create2.png}%
  {Generated ephemeral share link with one-click copy action}%
  {fig:secret_create_link}%
  {As shown in Figure~3.2, upon successful creation the interface presents the generated ephemeral share link alongside a one-click copy button, ready for distribution to the recipient.}%
  {Figure 3.2: Generated Ephemeral Share Link}%
  {Interface displaying the generated secret share URL with the one-click copy button.}

Once generated, the share link embeds the ephemeral secret identifier within the URL parameter. The sender distributes this URL to the intended recipient through any corporate communication channel, including chat applications or email, confident that intermediate network hops and message logs cannot decrypt the payload without access to the recipient's private device key.

\clearpage

Retrieving a secret likewise comprises two distinct operational phases designed to prevent accidental consumption by automated link crawlers, antivirus scanners, or unauthorized users. General-purpose link previews and security crawlers, such as Slack unfurlers and Microsoft Defender Safe Links, automatically execute HTTP GET requests against URLs appearing in messages. If retrieval were bound to link navigation, these automated background requests would permanently destroy the secret before the human recipient could access it.

To eliminate this vulnerability, navigation to a secret URL performs only a non-destructive existence check via \texttt{POST /api/secrets/exists}, which passes the identifier to the API only inside the JSON request body. The page request that opens the link still carries the identifier in its query string, a limitation discussed in Section~3.3.

\screenshot{images/screenshots/secret_reveal1.png}%
  {Burn warning notification modal prior to secret retrieval}%
  {fig:secret_reveal_warning}%
  {As shown in Figure~3.3, upon confirming the existence of the secret, the interface displays an explicit burn-after-reading modal dialog that warns the recipient that revealing the secret will permanently and irreversibly destroy it from the server.}%
  {Figure 3.3: Burn-After-Reading Warning Dialog}%
  {Modal warning dialogue alerting the recipient of permanent server-side destruction prior to retrieval.}

The modal requires deliberate recipient interaction, ensuring that secret consumption occurs only through conscious human initiation. If the recipient is not signed in, the dialogue first redirects to Keycloak, because every retrieval request must carry the recipient's access token.

\clearpage

After the recipient acknowledges the warning and authenticates through Keycloak, the client submits the atomic retrieval request via \texttt{POST /api/secrets/reveal}. The server executes the atomic Lua verification and single-delivery deletion in Redis, returning the ciphertext envelope. The browser unwraps the symmetric content key using its IndexedDB private key, decrypts the ciphertext in memory, and renders the plaintext.

\screenshot{images/screenshots/secret_reveal2.png}%
  {Decrypted plaintext secret revealed in browser memory}%
  {fig:secret_reveal_decrypted}%
  {As shown in Figure~3.4, the decrypted secret is displayed in a result panel below the retrieval form; the plaintext is held only in the memory of the page and is not written to browser storage.}%
  {Figure 3.4: Decrypted Secret Plaintext View}%
  {Interface displaying the decrypted secret plaintext in the result panel.}

To address the frontend security objectives mandated by the DAS guidelines, the web layer enforces four complementary client protections. First, a strict Content Security Policy dynamically configured in \texttt{web/middleware.ts} binds a fresh UUID nonce to every HTTP response, enforcing nonced \texttt{script-src} with \texttt{'strict-dynamic'} while explicitly disallowing \texttt{'unsafe-inline'} scripts to prevent script injection and key exfiltration. Second, anti-clickjacking controls (\texttt{frame-ancestors 'none'} and \texttt{X-Frame-Options: DENY}) prevent framing attacks intended to trigger unauthorized secret burning. Third, origin boundaries are secured via \texttt{base-uri 'none'}, \texttt{object-src 'none'}, and \texttt{form-action 'self'}. Finally, plaintext secrets exist solely in transient component memory, and after a successful decryption \texttt{window.history.replaceState} removes the identifier from the address bar; the browser history still keeps the original link.

These protections are verified by the test harness in \texttt{web/tests/security/csp.test.mjs}. The test suite executes two categories of assertions against production builds. As a negative control, it parses HTTP response headers to ensure constant baseline protections (\texttt{X-Frame-Options: DENY}, \texttt{X-Content-Type-Options: nosniff}, and \texttt{Referrer-Policy: no-referrer}) and verifies that every inline script emitted during Next.js hydration possesses the dynamic per-response nonce. As a positive control, the harness replays the real Content Security Policy header over a local HTTP server serving a test document with an injected, unnonced inline script. The test checks that the injected script carries no nonce and that the policy permits no \texttt{'unsafe-inline'} script; refusal by a real browser was confirmed in a manual headless Chromium run kept as evidence in the repository.

\subsec{Backend Architecture and API Routing}

The API is a FastAPI application built with Python 3.12. A Pydantic settings class validates the configuration at launch and rejects conflicting lifetime limits and invalid CIDR proxy blocks; missing OIDC or database settings are rejected only in production mode, which the compose files do not yet enable. Database schema migrations are executed automatically by Alembic during container initialization, halting startup upon migration failure.

\begin{table}[H]
\leadin{As shown in Table~3.1, the API exposes eight endpoints in addition to the generated OpenAPI documentation, and the identifier of a secret is accepted only in request bodies.}
\tablesetup
\caption{Endpoints of the API}
\label{tab:api}
\begin{tabularx}{\textwidth}{|P{0.38\textwidth}|P{0.11\textwidth}|L|}
\hline
\textbf{Endpoint} & \textbf{Auth} & \textbf{Behaviour} \\ \hline
\texttt{POST /api/keys/register} & token & registers a device public key and revokes the previous key of the same platform \\ \hline
\texttt{GET /api/keys/\{user\_id\}} & none & returns the active public keys of a user or 404 \\ \hline
\texttt{POST /api/secrets} & none & stores an envelope; 201 with identifier and expiry; creation rate limit \\ \hline
\texttt{POST /api/secrets/exists} & none & reports whether a secret exists without changing it \\ \hline
\texttt{POST /api/secrets/reveal} & token & 200 with envelope, 403 for another user (secret kept), 404 otherwise \\ \hline
\texttt{GET /api/health} & none & liveness check \\ \hline
\texttt{GET /api/me} & token & returns the caller's token claims (diagnostic) \\ \hline
\texttt{GET /api/} & none & static welcome message \\ \hline
\end{tabularx}
\end{table}

Errors are mapped centrally. Missing, burned and expired secrets yield one generic 404 response, so the history of an identifier cannot be enumerated. Redis connection loss produces a 503 response with a \texttt{Retry-After} header. Schema validation errors return 422 while strictly suppressing rejected payload bodies, ensuring malformed secrets are never echoed or persisted in external logs.

Three custom middleware layers wrap the application pipeline. A payload size filter validates declared lengths and received bytes, aborting requests above 65~KB (64~KB plus the JSON structure) with status 413 before parsing. A trusted proxy layer processes \texttt{X-Forwarded-For} headers strictly from designated CIDR ranges, selecting the right-most untrusted IP address to thwart client header spoofing. Additionally, a security header middleware injects \texttt{Cache-Control: no-store}, \texttt{X-Content-Type-Options: nosniff}, anti-framing headers, and restrictive CSP headers on all responses.

Rate limiting is enforced using fixed one-minute windows in Redis: clients are limited to 10 secret creations and 30 existence/reveal checks per minute (exceeding requests receive 429 with \texttt{Retry-After}). If Redis becomes unreachable, the rate limiter fails open with warning logs to preserve core service availability.

When a client registers a device public key, the endpoint revokes prior keys for that platform, which binds each user to the most recently registered browser. Secrets sent before a change of browser remain readable only on the previous browser.

\subsec{Security Controls}

The DAS brief requires teams to explain, for each security measure, the problem it addresses, why it is relevant, how it is implemented, how it was tested and what remains open \cite{dasbrief}. These explanations are kept in the repository for each control \cite{secretsharerepo} and are summarised here.

\begin{table}[H]
\leadin{As shown in Table~3.2, the controls go beyond the cryptographic core and cover logging, authentication and the web page itself.}
\tablesetup
\caption{Implemented security controls}
\label{tab:controls}
\begin{tabularx}{\textwidth}{|P{0.19\textwidth}|L|P{0.29\textwidth}|}
\hline
\textbf{Control} & \textbf{Implementation} & \textbf{Verification} \\ \hline
Identifiers kept out of request lines & reveal and existence check take the identifier in a JSON body; a log filter rewrites any \texttt{/secrets/<id>} path & a test fails if any secrets route gains a path parameter; captured-log tests \\ \hline
Audit without secret material & 8-character prefix, enforced also by a \texttt{varchar(8)} column and a CHECK constraint & insert of a full identifier rejected by PostgreSQL \\ \hline
Append-only audit log & database trigger raises on UPDATE and DELETE & UPDATE and DELETE fail, row unchanged \\ \hline
Audit does not weaken responses & audit write errors are logged and ignored & broken database still yields 201, 200 and 404 with identical bodies \\ \hline
Uniform authentication errors & one constant message for every invalid token, details only in the server log & six token faults give byte-identical bodies \\ \hline
Content Security Policy & per-request nonce, \texttt{strict-dynamic}, no \texttt{unsafe-inline} for scripts & automated policy test; blocked script confirmed in headless Chromium \\ \hline
Internal TLS & CA-verified TLS from the API to Redis and PostgreSQL & 18 tests of certificates and client settings \\ \hline
\end{tabularx}
\end{table}

The first control is the result of a vulnerability that the team found in its own code. An early version served secrets at \texttt{GET /secrets/\{id\}}, and during end-to-end testing the full 43-character identifier appeared in the Uvicorn access log, next to an audit table that carefully stored only 8 characters. Anyone with access to the logs or their backups could have read unconsumed secrets. The identifier was moved into the request body, which no access log records, and a logging filter was added as a second layer for old bookmarked links and for exception messages. This follows the general guidance that logs must not contain session identifiers, access tokens or sensitive data \cite{owasplogging}.

The Content Security Policy addresses the risk identified in Table~1.1: a script injected into the page could read the plaintext before encryption, which would defeat the whole design. The policy allows only scripts that carry a nonce generated for the current response, forbids plugins and framing, and restricts all connections to the same origin, which is possible because Traefik serves every component under one host \cite{csp3}. An automated test replays the real policy over a page containing an injected script and checks that the policy cannot permit it; a manual run in headless Chromium confirmed that the script does not run and that a violation event is raised.

The uniform authentication error closes an information leak in the token validation. Originally the 401 response contained the message of the underlying library, such as an unknown key identifier or an expired signature, which told an attacker which part of a forged token was wrong. Every rejected token now receives the same message, and the reason is written only to the server log, without the token itself. Error messages that reveal internal details are a typical example of the weaknesses listed by OWASP \cite{owasptop10}.

Several controls are known to be incomplete, and the team documented them transparently rather than claiming full operational maturity. In particular, the append-only trigger on the audit table can be disabled by the database owner, and the API currently connects using this owner role. Furthermore, while the audit log records client IP addresses and user agent headers, which qualify as personal data under the GDPR, no formal retention period or automated deletion job has been implemented yet \cite{gdpr}. On the web client, the Content Security Policy still permits inline styles required by the UI styling library, and the share link transmits the secret identifier in a query string, exposing it to the web tier before the page script strips it. Within the internal container network, TLS clients verify the CA certificate chain but do not enforce host-name verification for Redis and PostgreSQL, and the Redis datastore requires neither an access password nor client mTLS certificates. Finally, response timings across various endpoints were not benchmarked, meaning subtle processing time variations could theoretically reveal internal validation paths that the uniform HTTP payloads deliberately conceal.

% ------------------------------------------------------------------------------
\subsec{Testing and Continuous Delivery}

Testing was treated as part of every change: a security control was considered complete only when a test demonstrated both that the protection works and that the normal flow still works. The API test suite uses pytest with in-memory doubles for Redis where possible, a real Redis for the concurrency tests and a real PostgreSQL for the database guards. The in-memory store mirrors the Lua script, so that unit tests exercise the same logic as production.

\begin{table}[H]
\leadin{As shown in Table~3.3, the test suite is organised by concern; the first four modules form the security test package, the counts refer to test functions, and parametrised functions run several cases each.}
\tablesetup
\caption{Main groups of automated tests}
\label{tab:tests}
\begin{tabularx}{\textwidth}{|P{0.43\textwidth}|P{0.07\textwidth}|L|}
\hline
\textbf{Test module} & \textbf{Tests} & \textbf{What is verified} \\ \hline
\nolinkurl{test_secrets_invariants.py} & 16 & single delivery, denied reveal keeps the secret, 10 intruders racing 10 recipients \\ \hline
\nolinkurl{test_audit*.py} (3 modules) & 23 & events recorded, prefix only, database guards, responses unaffected by audit failures \\ \hline
\nolinkurl{test_access_log.py} & 9 & no identifier in routes or logs \\ \hline
\nolinkurl{test_auth_error_disclosure.py} & 7 & identical bodies for all token faults, no token in logs \\ \hline
\nolinkurl{test_secrets.py}, \nolinkurl{test_api.py} & 39 & validation, TTL bounds, size limit, error mapping \\ \hline
\nolinkurl{test_config.py}, \nolinkurl{test_proxy.py} & 38 & settings validation, trusted proxy handling \\ \hline
\nolinkurl{test_tls.py} & 18 & certificate generation, permissions, TLS client settings \\ \hline
\nolinkurl{test_oauth_integration.py} & 5 & live token flow against Keycloak \\ \hline
\nolinkurl{csp.test.mjs} (web client) & 10 & CSP headers, nonce uniqueness, policy refuses an injected script \\ \hline
\end{tabularx}
\end{table}

\begin{table}[H]
\leadin{Continuous integration runs on GitHub Actions. As shown in Table~3.4, four workflows cover testing and deployment of the application, and a fifth builds this report.}
\tablesetup
\caption{Continuous integration and deployment workflows}
\label{tab:ci}
\begin{tabularx}{\textwidth}{|P{0.20\textwidth}|P{0.26\textwidth}|L|}
\hline
\textbf{Workflow} & \textbf{Trigger} & \textbf{Steps} \\ \hline
Test API & pull requests; pushes to main, develop and feature branches & migrations, unit and security tests, failure if any security test is skipped; then the full stack in Docker with routing checks and live OAuth tests \\ \hline
Test TLS & same as Test API & certificate script and TLS client tests \\ \hline
PR Preview & pull request opened, updated or closed & deploys an isolated stack for the pull request over Tailscale SSH, runs smoke tests for routing, documentation, sign-in and the secret life cycle; removes the stack when the pull request closes \\ \hline
Deploy Staging & manual, main branch only & deploys main to staging; currently failing \\ \hline
Build Report & pushes to report branches & compiles the LaTeX report and publishes the PDF \\ \hline
\end{tabularx}
\end{table}

The run of the pipeline on 23 September 2026 for the latest commit of the main branch executed 176 unit and security tests with 5 skipped (the live OAuth tests, which run in the next job), 5 live OAuth integration tests and 18 TLS tests, all successfully. The CSP tests use the built-in Node.js test runner and are not yet part of the pipeline; they were run locally against the production Docker image.

The preview environments proved to be one of the most useful tools of the project. Every pull request receives its own copy of the whole system, reachable over HTTPS at a host name derived from the pull request number, so reviewers can test sign-in, encryption and the burn flow in a real browser before merging. The per-preview host name is also a technical necessity: the Web Cryptography API requires a secure context, and each preview needs its own redirect address registered in Keycloak.

% ------------------------------------------------------------------------------
\subsec{Challenges Encountered}

Several of the most instructive results of the internship were problems that the tests or the preview environments revealed. They are summarised here because each of them changed the design or the process.

The first problem was that a wrong recipient destroyed the secret. In an early version the reveal handler removed the envelope with \texttt{GETDEL} and only then compared the recipient. A user who opened a link meant for someone else received the expected 403 response, but when the real recipient opened the link, the secret was gone. The defect was reproduced with two accounts and fixed with the Lua script in Listing~2.1. The regression test now starts ten requests of a wrong user in parallel with ten requests of the recipient against a real Redis and asserts that exactly one request receives the secret and that it is one of the recipient's.

The second problem was the identifier in the access log, described in Section~3.3. It showed that a protection applied in one place, the truncated audit prefix, can be defeated by a component that the developers do not think of as storage, and it led to the rule that identifiers never appear in a URL of the API.

The third problem was an empty audit log. Everything appeared healthy and the smoke tests passed, but the \texttt{audit\_events} table did not exist in the preview and staging databases, because no step applied the migrations. The audit service ignores write errors by design, so that an audit outage cannot change the answers to secret requests, and the failure was therefore invisible. Migrations are now applied when the container starts, and the case shows that a deliberate fail-open decision must be paired with monitoring.

The fourth problem was a pipeline that reported success without running its tests. The preview deployment sent its script to the server over SSH through standard input, and a \texttt{docker compose exec} command inside the script read the rest of the script as its own input. The shell then reached the end of its input and exited with success before any smoke test had run, and the smoke tests had in the meantime become outdated without anyone noticing. The fix redirects the input of such commands and makes the step fail unless the log contains the final success message of the test battery.

The fifth problem concerned rate limiting behind proxies. All users of an environment shared one limit of ten creations per minute, and the audit log recorded the address of Traefik instead of the client, because the API saw every request as coming from the proxy. The trusted proxy middleware described in Section~3.2 solved the problem. The team also documented the pitfall that trusting the left-most \texttt{X-Forwarded-For} entry would let any client choose a new address for every request and bypass the limit.

The sixth problem appeared when the preview environments moved to HTTPS. Sign-in failed with an issuer mismatch, because Keycloak advertised its issuer as \texttt{https://<host>:80/keycloak/...}: Tailscale terminated HTTPS on port 443 and passed plain HTTP to Traefik on port 80, and Keycloak combined the forwarded scheme with the forwarded port. Since OpenID Connect clients must reject tokens whose issuer differs from the configured one \cite{oidccore}, the failure was correct behaviour of the client. It was fixed by making Traefik forward port 443 explicitly and by configuring the public host name of Keycloak for each preview.

A common lesson of these cases is that security properties must be tested end to end in an environment that resembles production. Each of the five problems passed unit tests at some point and was found only through an integration test, a preview deployment or a look at real logs.

% ==============================================================================
\unnumberedsection{CONCLUSIONS}

The domain analysis showed that credentials shared through chat and ticket systems remain exposed for years and that existing one-time-link services leave at least one of four weaknesses open. From it the team derived a concept in which the server is blind, access is bound to the recipient's identity, delivery is atomic and nothing secret is logged. The system design expressed the concept as nine functional and ten non-functional requirements, modelled the behaviour and structure in UML and summarised the security properties as five invariants that can be tested.

The principal achievement of the internship is a fully operational minimum viable product that guarantees zero-trust secret exchange. Browser-side hybrid encryption combines AES-256-GCM and RSA-OAEP so that the central application server stores and processes only opaque ciphertext envelopes. Recipient identity is rigorously enforced using Keycloak through OpenID Connect and the authorization code flow with PKCE, preventing unauthorized access even if share links are intercepted. Ephemeral storage and atomic single-delivery semantics are guaranteed by an atomic Redis Lua script that checks identity and deletes data in a single transactional step. Furthermore, internal testing exposed and eliminated sensitive identifier leakage from API request lines and server logs, establishing an append-only audit architecture that records only truncated prefixes and remains strictly decoupled from secret payload lifecycles. Finally, container-to-database communications are secured through internal CA-issued TLS certificates, the web tier is protected by a strict nonce-based Content Security Policy, and the entire system is validated by an automated continuous integration pipeline executing 176 tests alongside per-pull-request preview deployments.

The technical limitations identified during the development phase establish the development roadmap for the subsequent Project-Based Learning semester. On the client side, future iterations will send the active device identifier during the reveal phase to prevent burning a secret on an unregistered device, while device private keys will be generated as non-extractable CryptoKey instances. To mitigate user enumeration, public key retrieval will require prior authentication. In the identity tier, Keycloak will be hardened with multi-factor authentication and brute-force protection as specified by the DAS requirements. In the persistence layer, the API will be restricted to a non-owner database role, and audit records will be synchronized to immutable external storage, accompanied by automated GDPR-compliant data retention policies. In the frontend and network routing, secret identifiers will migrate from query parameters to URL hash fragments, and UI styling will be decoupled from inline attributes to allow an even stricter Content Security Policy. Lastly, staging deployment automation will be finalized, complemented by a formal incident response plan and threat mitigation protocol.

For the implementation phase, the data model already allows users from chat platforms such as Slack and Microsoft Teams, and a sender dashboard would let users revoke unread secrets.

In summary, the internship established a secure foundation for SecretShare: its central security properties are implemented, tested and documented together with their limits.

% ==============================================================================
\unnumberedsection{REFERENCES}
{\fontsize{11pt}{13pt}\selectfont\singlespacing
\bibliographystyle{IEEEtran}
\bibliography{references}
}

% ==============================================================================
% APPENDICES
% ==============================================================================
\startappendix{A}{Device Key Registration Sequence}

\diagram{seq_register}{Sequence diagram of device key registration}{fig:seqregister}{According to Figure~A.1, a browser registers its public key after the first sign-in, and the previous active key of the same platform is revoked in the same transaction, as described in Section~3.1.}

\startappendix{B}{Data Model}

\diagram{er}{Entity-relationship diagram of the PostgreSQL database}{fig:er}{According to Figure~B.1, the relational database contains three tables; private keys and ciphertext are never stored in PostgreSQL, and ciphertext exists only in Redis under a key with an expiry.}

\startappendix{C}{Browser Encryption Source Code}

The following functions from \texttt{web/lib/crypto.ts} encrypt a secret and wrap the content key, as described in Section~2.4. They use only the Web Cryptography API of the browser.

\begin{lstlisting}[language=TypeScript, caption={Encryption of the secret with AES-256-GCM}, label={lst:aes}, captionpos=b]
export async function encryptAesGcm(plaintext: string): Promise<{
  ciphertext: string
  iv: string
  rawAesKey: ArrayBuffer
}> {
  const rawAesKey = window.crypto.getRandomValues(new Uint8Array(32)) // 256-bit key
  const iv = window.crypto.getRandomValues(new Uint8Array(12)) // 96-bit IV

  const aesCryptoKey = await window.crypto.subtle.importKey(
    'raw', rawAesKey, { name: 'AES-GCM' }, false, ['encrypt']
  )
  const encodedPlaintext = new TextEncoder().encode(plaintext)
  const cipherBuffer = await window.crypto.subtle.encrypt(
    { name: 'AES-GCM', iv }, aesCryptoKey, encodedPlaintext
  )
  return {
    ciphertext: arrayBufferToBase64(cipherBuffer),
    iv: arrayBufferToBase64(iv.buffer),
    rawAesKey: rawAesKey.buffer,
  }
}
\end{lstlisting}

\begin{lstlisting}[language=TypeScript, caption={Wrapping of the content key with RSA-OAEP}, label={lst:wrap}, captionpos=b]
export async function encryptAesKeyWithRsa(
  rawAesKey: ArrayBuffer,
  rsaPublicKey: CryptoKey
): Promise<string> {
  const encryptedKeyBuffer = await window.crypto.subtle.encrypt(
    { name: 'RSA-OAEP' }, rsaPublicKey, rawAesKey
  )
  return arrayBufferToBase64(encryptedKeyBuffer)
}
\end{lstlisting}

\end{document}